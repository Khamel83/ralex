# Ralex Environment Configuration
# Copy to .env and configure with your values

# Required: OpenRouter API Key for model access
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Intelligence optimization (enables cost-first routing)
INTELLIGENCE_ENABLED=false

# Optional: Analytics and tracking
RALEX_ANALYTICS=false

# Optional: Development/debugging
DEBUG=false
LOG_LEVEL=INFO

# Optional: Custom model tier configuration
# (defaults loaded from .ralex/intelligence-config.yaml)
COST_TIER_CHEAP=gpt-3.5-turbo,claude-3-haiku
COST_TIER_MEDIUM=gpt-4,claude-3.5-sonnet  
COST_TIER_PREMIUM=gpt-4o,claude-3-opus

# Optional: Cost optimization limits
MAX_COST_PER_QUERY=0.10
DAILY_COST_BUDGET=5.00
WEEKLY_COST_BUDGET=25.00

# Optional: Network configuration
HOST=0.0.0.0
PORT=8000
WEBUI_PORT=3000

# Optional: Security
WEBUI_SECRET_KEY=ralex-secret-key

# Optional: Performance tuning for Raspberry Pi
WORKERS=1
TIMEOUT_KEEP_ALIVE=30

# Metrics: Actual measured performance (update with real values)
# Intelligence routing reduces API costs by X% vs baseline
# Setup completes in X minutes
# Routes queries in <X seconds average
# Uses X% fewer tokens per request